---
phase: 03-interactive-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/components/sections/CalculatorSection.tsx
  - src/components/sections/FAQSection.tsx
  - src/components/ui/accordion.tsx
  - package.json
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - FAQ-01
  - FAQ-02

must_haves:
  truths:
    - "Entering loan amount, interest rate, and term into the calculator instantly shows the estimated monthly payment"
    - "Calculator shows disclaimer text stating estimates only, not a commitment"
    - "Calculator has a CTA button that scrolls to the contact section"
    - "Clicking an FAQ item expands its answer; clicking another collapses the first"
    - "At least 6 FAQ items about the mortgage process are present"
  artifacts:
    - path: "src/components/sections/CalculatorSection.tsx"
      provides: "Interactive mortgage calculator with real-time P&I computation"
      contains: "use client"
    - path: "src/components/sections/FAQSection.tsx"
      provides: "FAQ accordion section with single-open behavior"
      contains: "Accordion"
    - path: "src/lib/constants.ts"
      provides: "FAQ_ITEMS array with 6-8 mortgage process questions"
      contains: "FAQ_ITEMS"
    - path: "src/components/ui/accordion.tsx"
      provides: "shadcn/ui Accordion component"
      contains: "AccordionTrigger"
  key_links:
    - from: "src/components/sections/FAQSection.tsx"
      to: "src/lib/constants.ts"
      via: "import FAQ_ITEMS"
      pattern: "import.*FAQ_ITEMS.*from.*constants"
    - from: "src/components/sections/FAQSection.tsx"
      to: "src/components/ui/accordion.tsx"
      via: "import Accordion components"
      pattern: "import.*Accordion.*from.*ui/accordion"
---

<objective>
Install dependencies, create the mortgage calculator and FAQ accordion sections as standalone components.

Purpose: Build the two simpler interactive sections (calculator and FAQ) so they are ready to be wired into the page. The calculator gives visitors a quick payment estimate; the FAQ addresses common hesitations before contacting Prashanna.

Output: CalculatorSection.tsx, FAQSection.tsx, FAQ_ITEMS data in constants.ts, shadcn accordion component installed.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-sections/03-CONTEXT.md
@.planning/phases/03-interactive-sections/03-RESEARCH.md

@src/lib/constants.ts
@src/components/layout/SectionWrapper.tsx
@src/components/ui/button.tsx
@src/app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/constants.ts:
```typescript
export const SECTION_IDS = {
  hero: "hero",
  services: "services",
  about: "about",
  testimonials: "testimonials",
  calculator: "calculator",
  faq: "faq",
  contact: "contact",
} as const;
```

From src/components/layout/SectionWrapper.tsx:
```typescript
interface SectionWrapperProps {
  id: string;
  children: React.ReactNode;
  className?: string;
  alternate?: boolean;
}
export function SectionWrapper({ id, children, className, alternate }: SectionWrapperProps)
```

From src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
// Button uses @base-ui/react render prop pattern, supports variant and size props
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add FAQ data to constants</name>
  <files>package.json, src/lib/constants.ts, src/components/ui/accordion.tsx</files>
  <action>
1. Install dependencies:
   ```bash
   npm install nodemailer zod
   npm install -D @types/nodemailer
   npx shadcn add accordion
   ```
   Note: nodemailer and zod are installed here for both Plan 01 and Plan 02 to avoid duplicate installs.

2. Add FAQ_ITEMS array to `src/lib/constants.ts` (append after TESTIMONIALS). Define an `FAQItem` interface with `question: string` and `answer: string`. Include 7 items focused on the mortgage loan process (per user decision: not product comparisons). Topics to cover:
   - What documents do I need to get started?
   - How long does the mortgage process take?
   - What credit score do I need?
   - What is pre-approval and why does it matter?
   - How much down payment do I need?
   - What are closing costs?
   - Can I get a mortgage if I'm self-employed?

   Each answer should be 2-3 sentences, informative but concise, written from Prashanna's perspective as a helpful loan officer.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && node -e "const c = require('./src/lib/constants.ts'); console.log('FAQ items:', c.FAQ_ITEMS?.length)" 2>/dev/null || npx tsx -e "import { FAQ_ITEMS } from './src/lib/constants'; console.log('FAQ items:', FAQ_ITEMS.length)"</automated>
  </verify>
  <done>shadcn accordion component exists at src/components/ui/accordion.tsx, nodemailer and zod in package.json dependencies, FAQ_ITEMS array with 7 items exported from constants.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create CalculatorSection component</name>
  <files>src/components/sections/CalculatorSection.tsx</files>
  <action>
Create `src/components/sections/CalculatorSection.tsx` as a `"use client"` component.

**Inputs (text fields, no sliders per user decision):**
- Loan amount: text input, default $300,000. Format with commas on blur, strip non-numeric for calculation.
- Interest rate: text input, default 6.5 (percent). Accept decimal values.
- Loan term: select or toggle between 15 and 30 years, default 30.

**Real-time calculation (no Calculate button per user decision):**
- Use `useState` for each input. Compute monthly payment on every change.
- Formula: M = P[r(1+r)^n] / [(1+r)^n - 1] where P=principal, r=monthly rate (annual/100/12), n=total payments (years*12).
- Handle edge cases: 0% interest (return principal / total months), invalid/empty inputs (show $0 or dash).

**Output display:**
- Show estimated monthly payment formatted as currency (e.g., "$1,896.20").
- Use large, prominent typography for the payment amount.

**Disclaimer (CALC-03):**
- Text below the result: "This calculator provides estimates only and does not constitute a commitment to lend. Actual rates, payments, and terms may vary. Contact me for a personalized quote."

**CTA button below disclaimer:**
- Text: "Get Your Personalized Rate" (or similar).
- Links to `#contact` using an anchor tag styled with Button component or buttonVariants.

**Layout:** Use SectionWrapper with `id={SECTION_IDS.calculator}`. No `alternate` prop (matches current placeholder). Style with semantic design tokens only (no hardcoded Tailwind colors). Mobile-responsive layout.

**Do NOT** add sliders, amortization schedules, or a "Calculate" button -- these are explicitly deferred/excluded.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build</automated>
  </verify>
  <done>CalculatorSection renders 3 inputs (loan amount, rate, term), computes and displays monthly P&I payment in real-time, shows disclaimer text, and has a CTA button linking to #contact</done>
</task>

<task type="auto">
  <name>Task 3: Create FAQSection component</name>
  <files>src/components/sections/FAQSection.tsx</files>
  <action>
Create `src/components/sections/FAQSection.tsx`.

This can be a Server Component (no `"use client"` needed) since shadcn Accordion handles its own client-side interactivity.

**Structure:**
- Use SectionWrapper with `id={SECTION_IDS.faq}` and `alternate` prop (matches current placeholder).
- Section heading: "Frequently Asked Questions" with supporting subtext like "Common questions about the mortgage process."
- Import `FAQ_ITEMS` from `@/lib/constants`.
- Import `Accordion, AccordionContent, AccordionItem, AccordionTrigger` from `@/components/ui/accordion`.

**Accordion behavior (per user decision: single-open):**
- Use `<Accordion type="single" collapsible>` so only one item is open at a time, and clicking the open item collapses it.
- Map over FAQ_ITEMS, rendering each as an AccordionItem with the question as AccordionTrigger and answer as AccordionContent.

**Styling:** Use semantic tokens. Style the heading consistently with other section headings (text-3xl font-bold, etc. matching ServicesSection/AboutSection pattern). Ensure the accordion items have clear visual hierarchy.

**Do NOT** add search, filtering, or categories to the FAQ -- keep it simple.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build</automated>
  </verify>
  <done>FAQSection renders a single-open accordion with all FAQ_ITEMS, matching the site's section heading pattern, with accessible expand/collapse behavior</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- CalculatorSection.tsx exists and exports a client component with real-time mortgage calculation
- FAQSection.tsx exists and uses shadcn Accordion with type="single" collapsible
- FAQ_ITEMS in constants.ts has at least 6 items
- No hardcoded Tailwind colors used (semantic tokens only)
</verification>

<success_criteria>
- Calculator accepts loan amount, interest rate, and term as inputs and displays a formatted monthly payment that updates in real-time
- Calculator includes disclaimer text and CTA button to contact section
- FAQ accordion opens one item at a time with 7 mortgage-process questions
- Both components use SectionWrapper and semantic design tokens
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-sections/03-01-SUMMARY.md`
</output>
