---
phase: 02-static-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/components/sections/HeroSection.tsx
  - src/components/sections/ServicesSection.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - SERV-01
  - SERV-02
  - SERV-03

must_haves:
  truths:
    - "Hero section displays a compelling headline, subtext, and a 'Get a Free Consultation' CTA button"
    - "Hero CTA button links to #contact and triggers smooth scroll"
    - "Hero shows a placeholder headshot (initials circle) in split layout"
    - "Services section displays all 11 loan type cards in a responsive grid"
    - "Each service card shows the loan type name, description, and 'Best for' audience"
    - "Clicking a service card scrolls to #contact"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "SERVICES and TESTIMONIALS data arrays with typed interfaces"
      contains: "SERVICES"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section with navy gradient, split layout, CTA button, placeholder headshot"
      min_lines: 25
    - path: "src/components/sections/ServicesSection.tsx"
      provides: "Services section with 11 loan type cards in responsive grid"
      min_lines: 30
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/lib/constants.ts"
      via: "imports SECTION_IDS"
      pattern: "SECTION_IDS"
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/lib/constants.ts"
      via: "imports SERVICES array and SECTION_IDS"
      pattern: "SERVICES"
    - from: "src/components/sections/HeroSection.tsx"
      to: "#contact"
      via: "anchor href"
      pattern: "href.*#contact"
    - from: "src/components/sections/ServicesSection.tsx"
      to: "#contact"
      via: "card anchor href"
      pattern: "href.*#contact"
---

<objective>
Create the data layer and two major content sections: Hero and Services.

Purpose: Establish the SERVICES and TESTIMONIALS typed data arrays in constants.ts, then build the HeroSection (navy gradient, split layout, CTA) and ServicesSection (11 loan type cards in responsive grid). These are the two highest-impact sections for visitor trust and conversion.

Output: Three files -- updated constants.ts with data arrays, HeroSection.tsx, ServicesSection.tsx
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-sections/02-CONTEXT.md
@.planning/phases/02-static-content-sections/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/layout/SectionWrapper.tsx:
```typescript
interface SectionWrapperProps {
  id: string;
  children: React.ReactNode;
  className?: string;
  alternate?: boolean;
}
export function SectionWrapper({ id, children, className, alternate }: SectionWrapperProps)
// Uses cn() to merge: "px-4 py-16 md:py-24", alternate ? "bg-secondary" : "bg-background", className
// Inner div: "mx-auto max-w-6xl"
```

From src/lib/constants.ts:
```typescript
export const SECTION_IDS = {
  hero: "hero",
  services: "services",
  about: "about",
  testimonials: "testimonials",
  calculator: "calculator",
  faq: "faq",
  contact: "contact",
} as const;

export const CONTACT = { phone: "(571) 222-5555", phoneHref: "tel:+15712225555", email: "prashanna@loanfactory.com", emailHref: "mailto:prashanna@loanfactory.com" } as const;
export const COMPLIANCE = { personalNmls: "NMLS #2528620", companyNmls: "Loan Factory, Inc. NMLS #320841", equalHousing: "Equal Housing Lender", disclaimer: "..." } as const;
```

From src/components/ui/button.tsx:
```typescript
// Uses @base-ui/react ButtonPrimitive, CVA variants
// Variants: default, outline, secondary, ghost, destructive, link
// Sizes: default, xs, sm, lg, icon, icon-xs, icon-sm, icon-lg
export { Button, buttonVariants }
```

Design tokens available (from globals.css @theme):
- Navy: `navy-50` through `navy-950` (hue 250 oklch)
- Gold: `gold-50` through `gold-950` (hue 65-85 oklch)
- Semantic: `bg-background`, `bg-card`, `bg-secondary`, `text-foreground`, `text-muted-foreground`, `text-card-foreground`, `bg-primary`, `text-primary-foreground`, `bg-accent`, `text-accent-foreground`, `border-border`
- Tailwind v4 gradient syntax: `bg-linear-to-*` (NOT `bg-gradient-to-*`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data constants and build HeroSection</name>
  <files>src/lib/constants.ts, src/components/sections/HeroSection.tsx</files>
  <action>
**1. Update `src/lib/constants.ts`** -- Add the following BELOW existing exports (do NOT modify existing CONTACT, COMPLIANCE, NAV_ITEMS, SOCIAL_LINKS, or SECTION_IDS):

Add `Service` interface: `{ name: string; slug: string; description: string; audience: string }`

Add `SERVICES` array with all 11 loan types (as const assertion):
- Conventional Loans (slug: "conventional") -- Traditional financing with competitive rates and flexible terms. Best for: Buyers with good credit and 3-20% down payment.
- FHA Loans (slug: "fha") -- Government-backed with lower down payment and credit requirements. Best for: First-time homebuyers and those with limited savings.
- VA Loans (slug: "va") -- Zero-down financing for eligible military service members. Best for: Active-duty military, veterans, and eligible spouses.
- USDA Loans (slug: "usda") -- Zero-down loans for homes in eligible rural and suburban areas. Best for: Buyers in qualifying rural/suburban locations.
- Jumbo Loans (slug: "jumbo") -- Financing above conforming loan limits for luxury properties. Best for: Buyers purchasing high-value homes.
- DSCR Loans (slug: "dscr") -- Investment property loans qualified by rental income, not personal income. Best for: Real estate investors and landlords.
- Bank Statement Loans (slug: "bank-statement") -- Self-employed borrower loans using bank statements instead of tax returns. Best for: Self-employed professionals and business owners.
- Foreign National Loans (slug: "foreign-national") -- Home financing for non-US citizens without a Social Security number. Best for: Foreign nationals buying US property.
- Refinancing (slug: "refinancing") -- Rate/term, cash-out, and streamline options to optimize your current mortgage. Best for: Homeowners seeking better rates or accessing equity.
- HELOC (slug: "heloc") -- Revolving line of credit using your home equity. Best for: Homeowners needing flexible access to funds.
- Reverse Mortgages (slug: "reverse-mortgage") -- Convert home equity into income without monthly mortgage payments. Best for: Homeowners age 62+ looking to supplement retirement income.

Write compelling 1-2 sentence descriptions. Keep audience lines concise starting with the buyer profile.

Add `Testimonial` interface: `{ name: string; context: string; quote: string }`

Add `TESTIMONIALS` array with 3 placeholder entries covering different loan types:
1. "John D." / "VA Home Purchase" / Quote about smooth VA loan process and patience with paperwork
2. "Maria S." / "First-Time FHA Buyer" / Quote about guidance through first home purchase, explaining every step
3. "Robert K." / "Cash-Out Refinance" / Quote about saving money through refinancing and responsiveness

Write authentic-sounding placeholder quotes (2-3 sentences each).

**2. Create `src/components/sections/HeroSection.tsx`** -- React Server Component (no "use client"):

- Import SectionWrapper and SECTION_IDS
- Use SectionWrapper with `id={SECTION_IDS.hero}` and `className="bg-linear-to-br from-navy-900 via-navy-800 to-navy-950 pt-20"` (this overrides SectionWrapper's default bg-background via tailwind-merge)
- Do NOT pass `alternate` prop (would conflict with gradient)
- Inner layout: `flex min-h-[calc(100svh-5rem)] flex-col items-center gap-8 md:flex-row md:items-center md:gap-12`
- Left side (flex-1, text-center on mobile, md:text-left):
  - h1: Hero headline (e.g., "Your Home Journey Starts Here") with `text-4xl font-bold tracking-tight text-white md:text-5xl lg:text-6xl`
  - p: Subtext (e.g., "Expert mortgage guidance tailored to your goals. From first-time buyers to seasoned investors, I'll find the right loan for you.") with `mt-4 text-lg text-navy-200 md:text-xl`
  - CTA: `<a href="#contact">` styled as a button: `mt-8 inline-flex h-12 items-center rounded-lg bg-gold-500 px-8 text-base font-semibold text-navy-950 transition-colors hover:bg-gold-400` with text "Get a Free Consultation"
- Right side (shrink-0, flex items-center justify-center):
  - Placeholder headshot: `<div>` styled as a large circle with initials "PS": `flex size-56 items-center justify-center rounded-full border-4 border-gold-500/30 bg-navy-700 text-5xl font-bold text-gold-400 md:size-72 lg:size-80`
- On mobile: stacks vertically (text on top, headshot below) -- this is handled by flex-col + md:flex-row
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>constants.ts has Service interface, SERVICES array with 11 entries, Testimonial interface, TESTIMONIALS array with 3 entries. HeroSection.tsx renders a navy gradient hero with split layout, compelling headline, subtext, gold CTA button linking to #contact, and placeholder headshot circle.</done>
</task>

<task type="auto">
  <name>Task 2: Build ServicesSection with responsive card grid</name>
  <files>src/components/sections/ServicesSection.tsx</files>
  <action>
Create `src/components/sections/ServicesSection.tsx` -- React Server Component (no "use client"):

- Import SectionWrapper, SECTION_IDS, and SERVICES from constants
- Use SectionWrapper with `id={SECTION_IDS.services}` and `alternate` prop (for bg-secondary alternating pattern, matching current page.tsx)
- Section header: centered h2 "Loan Programs" or "Mortgage Solutions" with `text-3xl font-bold text-foreground md:text-4xl` and a subheading p below like "Explore the right financing option for your situation" with `mt-3 text-lg text-muted-foreground`
- Responsive grid: `grid grid-cols-1 gap-6 mt-12 md:grid-cols-2 lg:grid-cols-3`
- Each service card is an `<a href="#contact">` tag (entire card clickable, scrolls to contact section):
  - Container: `group block rounded-xl border border-border border-t-4 border-t-gold-500 bg-card p-6 shadow-sm transition-all hover:shadow-md hover:-translate-y-0.5`
  - Add `data-service={service.slug}` attribute on each card for Phase 3 pre-selection
  - h3: service.name with `text-lg font-semibold text-card-foreground group-hover:text-gold-700`
  - p: service.description with `mt-2 text-sm leading-relaxed text-muted-foreground`
  - p: "Best for: {service.audience}" with `mt-3 text-sm font-medium text-gold-700`
- Map over SERVICES array using service.slug as the key
- No icons on cards (per CLAUDE.md minimal icons rule)
- Gold top border accent is the visual differentiator per locked decision
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ServicesSection.tsx renders all 11 loan type cards in a responsive grid (1 col mobile, 2 tablet, 3 desktop). Each card shows name, description, and audience with gold top border accent. Each card is an anchor link to #contact with data-service attribute for future pre-selection.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `npm run lint` passes
3. SERVICES array has exactly 11 entries with name, slug, description, audience fields
4. TESTIMONIALS array has exactly 3 entries with name, context, quote fields
5. HeroSection uses bg-linear-to-br (not bg-gradient-to-br) for Tailwind v4
6. No "use client" directive in any section component
7. No hardcoded Tailwind colors (bg-blue-*, text-red-*, etc.) -- only design tokens
</verification>

<success_criteria>
- constants.ts exports Service interface, SERVICES (11 items), Testimonial interface, and TESTIMONIALS (3 items)
- HeroSection.tsx is a Server Component rendering navy gradient hero with split layout, headline, subtext, gold CTA linking to #contact, and PS initials placeholder
- ServicesSection.tsx is a Server Component rendering 11 service cards in responsive grid with gold top border accent and anchor links to #contact
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-sections/02-01-SUMMARY.md`
</output>
