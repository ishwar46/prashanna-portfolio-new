---
phase: 02-static-content-sections
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/sections/AboutSection.tsx
  - src/components/sections/TestimonialsSection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "About section displays Prashanna's bio with a personal journey tone and a placeholder photo"
    - "About section includes 2-3 credibility stats/highlights below the bio"
    - "Testimonials section shows 3 client review cards in a responsive grid"
    - "Each testimonial card shows client name (First LastInitial.), loan type context, and quote"
    - "All 4 sections (Hero, Services, About, Testimonials) render on the page with no placeholder text remaining for Phase 2 sections"
  artifacts:
    - path: "src/components/sections/AboutSection.tsx"
      provides: "About section with bio, placeholder photo, and stat highlights"
      min_lines: 30
    - path: "src/components/sections/TestimonialsSection.tsx"
      provides: "Testimonials section with 3 client review cards"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Page composition importing all 4 section components"
      contains: "HeroSection"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "component import"
      pattern: "import.*HeroSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/ServicesSection.tsx"
      via: "component import"
      pattern: "import.*ServicesSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/AboutSection.tsx"
      via: "component import"
      pattern: "import.*AboutSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/TestimonialsSection.tsx"
      via: "component import"
      pattern: "import.*TestimonialsSection"
    - from: "src/components/sections/TestimonialsSection.tsx"
      to: "src/lib/constants.ts"
      via: "imports TESTIMONIALS array"
      pattern: "TESTIMONIALS"
---

<objective>
Build the About and Testimonials sections, then wire all four Phase 2 section components into page.tsx, replacing the remaining placeholder content.

Purpose: Complete the static content layer so visitors can read Prashanna's story, see credibility stats, and review client testimonials. The page assembly step replaces all Phase 2 placeholder sections with real components.

Output: AboutSection.tsx, TestimonialsSection.tsx, and updated page.tsx with all section components composed
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-sections/02-CONTEXT.md
@.planning/phases/02-static-content-sections/02-RESEARCH.md
@.planning/phases/02-static-content-sections/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/constants.ts (after Plan 01):
```typescript
export interface Service {
  name: string;
  slug: string;
  description: string;
  audience: string;
}
export const SERVICES: Service[];  // 11 entries

export interface Testimonial {
  name: string;
  context: string;
  quote: string;
}
export const TESTIMONIALS: Testimonial[];  // 3 entries

export const SECTION_IDS = {
  hero: "hero", services: "services", about: "about",
  testimonials: "testimonials", calculator: "calculator",
  faq: "faq", contact: "contact",
} as const;

export const CONTACT = { phone: "(571) 222-5555", ... } as const;
export const COMPLIANCE = { personalNmls: "NMLS #2528620", ... } as const;
```

From src/components/layout/SectionWrapper.tsx:
```typescript
interface SectionWrapperProps {
  id: string;
  children: React.ReactNode;
  className?: string;
  alternate?: boolean;
}
export function SectionWrapper({ id, children, className, alternate }: SectionWrapperProps)
// cn("px-4 py-16 md:py-24", alternate ? "bg-secondary" : "bg-background", className)
// Inner: "mx-auto max-w-6xl"
```

From src/components/sections/HeroSection.tsx (Plan 01):
```typescript
export function HeroSection()  // Server Component, no props
```

From src/components/sections/ServicesSection.tsx (Plan 01):
```typescript
export function ServicesSection()  // Server Component, no props
```

Design tokens: navy-50..950, gold-50..950, bg-background, bg-card, bg-secondary, text-foreground, text-muted-foreground, text-card-foreground, border-border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AboutSection and TestimonialsSection</name>
  <files>src/components/sections/AboutSection.tsx, src/components/sections/TestimonialsSection.tsx</files>
  <action>
**1. Create `src/components/sections/AboutSection.tsx`** -- React Server Component (no "use client"):

- Import SectionWrapper and SECTION_IDS from constants
- Use SectionWrapper with `id={SECTION_IDS.about}` (no `alternate` -- matches current page.tsx which uses plain bg-background for about)
- Side-by-side layout: `flex flex-col items-center gap-8 md:flex-row md:gap-12 lg:gap-16`
- Left side (photo placeholder, shrink-0):
  - Placeholder avatar matching hero headshot style: a large circle with initials "PS"
  - Style: `flex size-48 items-center justify-center rounded-full bg-navy-100 text-4xl font-bold text-navy-700 md:size-64`
  - On mobile: photo on top (handled by flex-col)
- Right side (flex-1):
  - h2: "About Prashanna" with `text-3xl font-bold text-foreground md:text-4xl`
  - Bio paragraphs (2-3 paragraphs) with warm, personal journey tone:
    - Paragraph 1: Opening about passion for helping people achieve homeownership, why he got into mortgage lending
    - Paragraph 2: Experience at Loan Factory, range of loan programs (conventional to specialized), serving diverse borrowers
    - Paragraph 3: Personal commitment to transparency, education-first approach, making the process stress-free
  - Use `mt-4 text-muted-foreground leading-relaxed` for paragraph styling, `space-y-4` on the paragraph container
  - Below the bio, add 2-3 stat/highlight boxes in a flex row:
    - Container: `mt-8 flex flex-wrap gap-4`
    - Each stat box: `rounded-lg bg-secondary px-6 py-4 text-center`
    - Stat number/value: `text-2xl font-bold text-navy-900` (e.g., "11", "500+", "100%")
    - Stat label: `text-sm text-muted-foreground` (e.g., "Loan Programs", "Families Helped", "Client Focus")
    - Use 3 stats: "11 Loan Programs", "500+ Families Helped", "100% Client Focus"

**2. Create `src/components/sections/TestimonialsSection.tsx`** -- React Server Component (no "use client"):

- Import SectionWrapper, SECTION_IDS, and TESTIMONIALS from constants
- Use SectionWrapper with `id={SECTION_IDS.testimonials}` and `alternate` prop (matches current page.tsx)
- Section header: centered h2 "What Clients Say" with `text-3xl font-bold text-foreground md:text-4xl` and subheading p like "Hear from homeowners I've helped" with `mt-3 text-lg text-muted-foreground`
- Grid: `grid grid-cols-1 gap-6 mt-12 md:grid-cols-3`
- Each testimonial card:
  - Container: `rounded-xl bg-card p-6 shadow-sm border border-border`
  - Large decorative open-quote character at top: a `<span>` with `text-4xl leading-none text-gold-500 font-serif` containing the left double quotation mark character (Unicode \u201C or just use the character directly)
  - Quote text: `mt-2 text-foreground leading-relaxed` wrapping testimonial.quote
  - Bottom section with divider: `mt-6 border-t border-border pt-4`
    - Client name: `font-semibold text-foreground` (e.g., "John D.")
    - Loan context: `text-sm text-muted-foreground` (e.g., "VA Home Purchase")
- Map over TESTIMONIALS array using testimonial.name as key
- All 3 testimonials visible at once (no carousel per locked decision)
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AboutSection.tsx renders side-by-side layout with placeholder photo, bio text, and 3 stat highlights. TestimonialsSection.tsx renders 3 testimonial cards in responsive grid with quote, client name, and loan type context. Both are Server Components using design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all section components into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Update `src/app/page.tsx` to replace Phase 2 placeholder sections with real components:

1. Add imports at top:
   ```
   import { HeroSection } from "@/components/sections/HeroSection";
   import { ServicesSection } from "@/components/sections/ServicesSection";
   import { AboutSection } from "@/components/sections/AboutSection";
   import { TestimonialsSection } from "@/components/sections/TestimonialsSection";
   ```

2. Replace the first 4 SectionWrapper blocks (hero, services, about, testimonials) with the corresponding component tags:
   - `<HeroSection />` replaces the hero SectionWrapper block
   - `<ServicesSection />` replaces the services SectionWrapper block
   - `<AboutSection />` replaces the about SectionWrapper block
   - `<TestimonialsSection />` replaces the testimonials SectionWrapper block

3. KEEP the remaining 3 placeholder SectionWrapper blocks intact (calculator, faq, contact -- these are Phase 3 scope)

4. KEEP the existing imports for SectionWrapper and SECTION_IDS (still needed for the Phase 3 placeholder sections)

The final page.tsx should render: HeroSection, ServicesSection, AboutSection, TestimonialsSection, then 3 remaining placeholder SectionWrappers for calculator, faq, and contact.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>page.tsx imports and renders all 4 Phase 2 section components in order (Hero, Services, About, Testimonials) followed by 3 Phase 3 placeholder sections. No Phase 2 placeholder text ("Coming in Phase 2") remains. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `npm run lint` passes
3. page.tsx imports HeroSection, ServicesSection, AboutSection, TestimonialsSection
4. No "Coming in Phase 2" placeholder text remains in page.tsx
5. Phase 3 placeholder sections (calculator, faq, contact) still present
6. No "use client" in any new section component
7. No hardcoded Tailwind colors -- only design tokens and navy/gold palette
8. TestimonialsSection reads from TESTIMONIALS constant, not inline data
</verification>

<success_criteria>
- AboutSection.tsx renders with placeholder photo, bio, and 3 stat highlights
- TestimonialsSection.tsx renders 3 testimonial cards with name, context, and quote
- page.tsx composes all 4 Phase 2 sections plus 3 Phase 3 placeholders
- `npm run build` passes
- No Phase 2 placeholder content remains visible
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-sections/02-02-SUMMARY.md`
</output>
