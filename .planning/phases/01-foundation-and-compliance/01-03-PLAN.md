---
phase: 01-foundation-and-compliance
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/Footer.tsx
autonomous: false
requirements:
  - NAV-05
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Footer displays personal NMLS #2528620"
    - "Footer displays company NMLS (Loan Factory, Inc. NMLS #320841)"
    - "Footer displays 'Equal Housing Lender' text with a house icon"
    - "Footer displays the legal disclaimer about not being a commitment to lend"
    - "Footer contains quick navigation links to Services, About, Calculator, and Contact sections"
    - "Footer displays phone number as a tel: link and email as a mailto: link"
    - "Footer contains social media links for Instagram, Facebook, and LinkedIn"
    - "Footer displays a copyright notice with the current year"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Full footer with compliance disclosures, contact info, social links, quick nav, and copyright"
      min_lines: 60
      exports: ["Footer"]
  key_links:
    - from: "src/components/layout/Footer.tsx"
      to: "src/lib/constants.ts"
      via: "imports CONTACT, COMPLIANCE, NAV_ITEMS, SOCIAL_LINKS"
      pattern: "import.*constants"

user_setup: []
---

<objective>
Build the complete footer with all NMLS/compliance disclosures, contact information, social links, quick navigation, and copyright notice.

Purpose: The footer is the compliance anchor of the site -- mortgage regulations require visible NMLS numbers, Equal Housing Lender notice, and lending disclaimers. It also provides secondary navigation and contact channels for visitors who scroll to the bottom.

Output: A production-ready Footer.tsx that replaces the Plan 01 stub, containing all legally required disclosures and useful footer content.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-compliance/01-CONTEXT.md
@.planning/phases/01-foundation-and-compliance/01-RESEARCH.md
@.planning/phases/01-foundation-and-compliance/01-01-SUMMARY.md

@src/lib/constants.ts
@src/lib/utils.ts
@src/app/globals.css

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From src/lib/constants.ts:
```typescript
export const CONTACT = {
  phone: "(571) 222-5555",
  phoneHref: "tel:+15712225555",
  email: "prashanna@loanfactory.com",
  emailHref: "mailto:prashanna@loanfactory.com",
} as const;

export const COMPLIANCE = {
  personalNmls: "NMLS #2528620",
  companyNmls: "Loan Factory, Inc. NMLS #320841",
  equalHousing: "Equal Housing Lender",
  disclaimer: "Not a commitment to lend. Programs, rates, terms, and conditions are subject to change without notice. All loans subject to credit approval.",
} as const;

export const NAV_ITEMS = [
  { label: "Services", href: "#services" },
  { label: "About", href: "#about" },
  { label: "Testimonials", href: "#testimonials" },
  { label: "Calculator", href: "#calculator" },
  { label: "FAQ", href: "#faq" },
  { label: "Contact", href: "#contact" },
] as const;

export const SOCIAL_LINKS = {
  instagram: "https://instagram.com/prashannasangroula",
  facebook: "https://facebook.com/prashannasangroula",
  linkedin: "https://linkedin.com/in/prashannasangroula",
} as const;
```

Design tokens available (from globals.css @theme):
- Navy: bg-navy-50 through bg-navy-950, text-navy-*, border-navy-*
- Gold: bg-gold-50 through bg-gold-900, text-gold-*, border-gold-*
- Semantic: bg-primary (navy-900), text-accent (gold-500), bg-secondary (light navy tint)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the complete Footer component with compliance disclosures</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
**Replace the stub `src/components/layout/Footer.tsx`** with the full implementation as a Server Component (no "use client" directive -- the footer is entirely static).

Imports: `CONTACT`, `COMPLIANCE`, `NAV_ITEMS`, `SOCIAL_LINKS` from `@/lib/constants`. `Phone`, `Mail`, `Home` icons from `lucide-react`. Optionally `Instagram`, `Facebook`, `Linkedin` icons from `lucide-react` for social links (if available in lucide -- check; if not, use text labels).

**Footer structure:** `<footer className="bg-navy-950 text-navy-200">` with two major sections:

**Section 1: Main footer content** -- `<div className="mx-auto max-w-6xl px-4 py-12">` containing a responsive grid `<div className="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">`:

**Column 1 -- Brand:**
- `<h3 className="text-lg font-bold text-white">Prashanna Sangroula</h3>`
- `<p className="mt-1 text-xs text-navy-400">{COMPLIANCE.personalNmls}</p>`
- `<p className="mt-3 text-sm">Mortgage Loan Officer</p>`
- `<p className="text-sm">Loan Factory, Inc.</p>`

**Column 2 -- Quick Links:**
- `<h4 className="font-semibold text-white">Quick Links</h4>`
- `<nav className="mt-3 flex flex-col gap-2">` mapping a subset of NAV_ITEMS: Services, About, Calculator, Contact (per user decision: "repeat section anchor links (Services, About, Calculator, Contact)"). Render as `<a href={item.href} className="text-sm text-navy-300 transition-colors hover:text-gold-400">{item.label}</a>`

**Column 3 -- Contact:**
- `<h4 className="font-semibold text-white">Contact</h4>`
- `<div className="mt-3 flex flex-col gap-2">`
- Phone: `<a href={CONTACT.phoneHref} className="flex items-center gap-2 text-sm text-navy-300 transition-colors hover:text-gold-400">` with `<Phone className="size-4" />` and `{CONTACT.phone}`
- Email: `<a href={CONTACT.emailHref} className="flex items-center gap-2 text-sm text-navy-300 transition-colors hover:text-gold-400">` with `<Mail className="size-4" />` and `{CONTACT.email}`

**Column 4 -- Social / Connect:**
- `<h4 className="font-semibold text-white">Connect</h4>`
- `<div className="mt-3 flex gap-3">` with social links. For each entry in SOCIAL_LINKS, render `<a href={url} target="_blank" rel="noopener noreferrer" aria-label="{platform}" className="text-navy-300 transition-colors hover:text-gold-400">` with the appropriate Lucide icon (`Instagram`, `Facebook`, `Linkedin` -- all available in lucide-react) at `className="size-5"`.

**Section 2: Compliance bar** -- `<div className="border-t border-navy-800">` then `<div className="mx-auto max-w-6xl px-4 py-6">`:

- **Equal Housing + NMLS row:** `<div className="flex flex-wrap items-center justify-center gap-x-4 gap-y-2 text-center text-xs text-navy-400">`
  - `<span className="flex items-center gap-1">` with `<Home className="size-3.5" />` and `{COMPLIANCE.equalHousing}` (using Lucide Home icon per research recommendation)
  - `<span>{COMPLIANCE.personalNmls}</span>`
  - `<span>{COMPLIANCE.companyNmls}</span>`

- **Legal disclaimer:** `<p className="mt-3 text-center text-xs leading-relaxed text-navy-500">{COMPLIANCE.disclaimer}</p>`

- **Copyright:** `<p className="mt-3 text-center text-xs text-navy-500">&copy; {new Date().getFullYear()} Prashanna Sangroula. All rights reserved.</p>`

**Key details per user decisions:**
- Footer uses COMPLIANCE constants -- no inline string literals for NMLS or disclaimer
- Equal Housing Lender uses Lucide Home icon + text (not an external image asset)
- Social links point to placeholder URLs (will be updated when confirmed)
- Footer should feel substantial but not overwhelming -- 4-column grid on desktop, stacking on mobile
- Dark navy-950 background creates clear visual separation from page content
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>Footer.tsx is a Server Component with 4-column responsive grid (brand, quick links, contact, social) plus a compliance bar containing Equal Housing Lender with icon, both NMLS numbers, the legal disclaimer, and copyright. All text sourced from constants.ts. Build and lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 foundation in browser</name>
  <action>
    Human verifies the complete Phase 1 output across all three plans by inspecting the running application in the browser.
  </action>
  <what-built>Complete Phase 1 foundation: design token system (navy/gold), sticky header with transparent-to-solid scroll behavior and mobile hamburger menu, footer with all compliance disclosures, and page with 7 section placeholders.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in a browser
    2. **Header check:** Verify the header shows "Prashanna Sangroula" wordmark, NMLS #2528620, nav links (Services, About, etc.), and phone number (571) 222-5555
    3. **Scroll behavior:** Scroll down the page -- the header should transition from transparent to solid navy with a shadow
    4. **Nav links:** Click "Services" or "Contact" in the header -- the page should smooth-scroll to the correct section placeholder
    5. **Mobile check:** Resize the browser to a narrow viewport (< 768px). Nav links should be replaced by a hamburger icon. Phone number should remain visible. Tap the hamburger -- a side panel should slide in with all nav links. Tap a link -- the panel should close and scroll to the section.
    6. **Phone link:** Click/tap the phone number -- it should trigger a call action (on mobile) or show the tel: protocol prompt
    7. **Footer check:** Scroll to the bottom. Verify the footer shows:
       - "Prashanna Sangroula" with NMLS #2528620
       - Quick links (Services, About, Calculator, Contact)
       - Phone and email contact info
       - Instagram, Facebook, LinkedIn social links
       - Equal Housing Lender text with house icon
       - Company NMLS (Loan Factory, Inc. NMLS #320841)
       - Legal disclaimer text
       - Copyright notice
    8. **Alternating sections:** Verify the 7 section placeholders alternate between white and light backgrounds
  </how-to-verify>
  <verify>Visual inspection in browser per steps above</verify>
  <done>All Phase 1 roadmap success criteria confirmed by visual inspection</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes with zero errors
- Footer.tsx imports CONTACT, COMPLIANCE, NAV_ITEMS, SOCIAL_LINKS from constants.ts (no inline strings)
- Footer contains all four compliance requirements: NMLS #2528620, Company NMLS #320841, Equal Housing Lender, legal disclaimer
- Footer has social links, contact info, quick nav links, and copyright
- Visual verification via human checkpoint covers all Phase 1 success criteria
</verification>

<success_criteria>
The footer displays all required compliance disclosures (personal NMLS, company NMLS, Equal Housing Lender, legal disclaimer), contact information (phone + email), social links, quick navigation, and copyright. Combined with the header from Plan 02, all Phase 1 success criteria from the roadmap are met. Human verification confirms the complete visual and functional experience.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-compliance/01-03-SUMMARY.md`
</output>
