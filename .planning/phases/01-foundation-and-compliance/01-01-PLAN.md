---
phase: 01-foundation-and-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/constants.ts
  - src/components/layout/SectionWrapper.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Design tokens for navy and gold brand colors generate working Tailwind utility classes (bg-navy-900, text-gold-500, etc.)"
    - "Semantic shadcn tokens (--primary, --accent, --secondary) are mapped to the navy/gold palette"
    - "CSS smooth scroll is enabled globally with scroll-padding-top offset for the fixed header"
    - "All seven section anchors exist on the page (hero, services, about, testimonials, calculator, faq, contact)"
    - "Clicking a hash link in the browser address bar scrolls to the correct section with smooth behavior"
    - "Root layout renders Header, main content, and Footer in correct order"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Navy/gold design tokens, smooth scroll CSS, semantic token overrides"
      contains: "color-navy"
    - path: "src/lib/constants.ts"
      provides: "Single source of truth for contact info, NMLS numbers, nav items, compliance text, social links"
      exports: ["CONTACT", "COMPLIANCE", "NAV_ITEMS", "SOCIAL_LINKS"]
    - path: "src/components/layout/SectionWrapper.tsx"
      provides: "Reusable section container with id, alternating backgrounds, max-width constraint"
      exports: ["SectionWrapper"]
    - path: "src/app/page.tsx"
      provides: "Home page composing all section placeholders with correct IDs"
      contains: "id=\"hero\""
    - path: "src/app/layout.tsx"
      provides: "Root layout with Header, Footer, metadata, smooth scroll data attribute"
      contains: "data-scroll-behavior"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/SectionWrapper.tsx"
      via: "import and render with section IDs"
      pattern: "SectionWrapper.*id="
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render in body"
      pattern: "<Header"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import and render in body"
      pattern: "<Footer"
---

<objective>
Establish the design system, shared constants, reusable section wrapper, and full page scaffolding that all subsequent plans build on.

Purpose: Every other plan in this phase (Header, Footer) and all future phases (Hero, Services, etc.) depend on the navy/gold design tokens, the constants file, the SectionWrapper pattern, and the layout.tsx wiring. Building this first eliminates coupling between the Header and Footer plans so they can execute in parallel.

Output: Working design token system, constants file, SectionWrapper component, page with 7 section anchors, and root layout wired with stub Header/Footer components ready for Plans 02 and 03 to replace.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-compliance/01-CONTEXT.md
@.planning/phases/01-foundation-and-compliance/01-RESEARCH.md

@src/app/globals.css
@src/app/layout.tsx
@src/app/page.tsx
@src/lib/utils.ts
@src/components/ui/button.tsx

<interfaces>
<!-- Existing codebase interfaces the executor needs -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
// Button uses @base-ui/react/button, CVA variants: default, outline, secondary, ghost, destructive, link
// Sizes: default, xs, sm, lg, icon, icon-xs, icon-sm, icon-lg
```

From components.json:
```json
{ "style": "base-nova", "rsc": true, "tsx": true, "iconLibrary": "lucide" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants file and extend design tokens</name>
  <files>src/lib/constants.ts, src/app/globals.css</files>
  <action>
**1. Create `src/lib/constants.ts`** with these exact exports:

- `CONTACT` object: `phone: "(571) 222-5555"`, `phoneHref: "tel:+15712225555"`, `email: "prashanna@loanfactory.com"`, `emailHref: "mailto:prashanna@loanfactory.com"` -- all `as const`
- `COMPLIANCE` object: `personalNmls: "NMLS #2528620"`, `companyNmls: "Loan Factory, Inc. NMLS #320841"`, `equalHousing: "Equal Housing Lender"`, `disclaimer: "Not a commitment to lend. Programs, rates, terms, and conditions are subject to change without notice. All loans subject to credit approval."` -- all `as const`
- `NAV_ITEMS` array: `[{ label: "Services", href: "#services" }, { label: "About", href: "#about" }, { label: "Testimonials", href: "#testimonials" }, { label: "Calculator", href: "#calculator" }, { label: "FAQ", href: "#faq" }, { label: "Contact", href: "#contact" }]` -- `as const`
- `SOCIAL_LINKS` object: `instagram: "https://instagram.com/prashannasangroula"`, `facebook: "https://facebook.com/prashannasangroula"`, `linkedin: "https://linkedin.com/in/prashannasangroula"` -- `as const` (placeholder URLs per research open questions)
- `SECTION_IDS` object: `{ hero: "hero", services: "services", about: "about", testimonials: "testimonials", calculator: "calculator", faq: "faq", contact: "contact" }` -- `as const`

**2. Extend `src/app/globals.css`** with navy/gold design tokens.

Add a new `@theme { ... }` block (NOT inside the existing `@theme inline` block) with the full navy color scale (50-950, hue ~250) and gold color scale (50-900, hue ~65-85) using oklch values from the research document Pattern 1.

Then update the `:root` block to remap semantic tokens to the brand palette:
- `--primary`: navy-900 value `oklch(0.20 0.07 250)` (dark navy for primary actions)
- `--primary-foreground`: white `oklch(0.98 0 0)`
- `--accent`: gold-500 value `oklch(0.72 0.15 70)` (gold for accent/CTA highlights)
- `--accent-foreground`: navy-900 value `oklch(0.20 0.07 250)`
- `--secondary`: very light navy tint `oklch(0.97 0.005 250)` (alternating section backgrounds)
- `--secondary-foreground`: navy-900 value `oklch(0.20 0.07 250)`

Add smooth scroll rules to the `@layer base` block:
```css
html {
  scroll-behavior: smooth;
  scroll-padding-top: 5rem;
}
```
Keep the existing `html { @apply font-sans; }` rule -- merge them into one `html` selector.

Do NOT modify the `.dark` block or the `@theme inline` block. Do NOT remove any existing CSS variables. Only add and override.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>constants.ts exports CONTACT, COMPLIANCE, NAV_ITEMS, SOCIAL_LINKS, SECTION_IDS. globals.css has navy-50 through navy-950 and gold-50 through gold-900 color tokens. Semantic tokens --primary, --accent, --secondary are remapped to navy/gold. Smooth scroll with 5rem padding is enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create SectionWrapper, stub layout components, wire layout and page</name>
  <files>src/components/layout/SectionWrapper.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
**1. Create `src/components/layout/SectionWrapper.tsx`** as a Server Component (no "use client" directive):

```typescript
interface SectionWrapperProps {
  id: string;
  children: React.ReactNode;
  className?: string;
  alternate?: boolean;
}
```

Renders a `<section>` with the `id` prop, applies `cn("px-4 py-16 md:py-24", alternate ? "bg-secondary" : "bg-background", className)`. Inside the section, wraps children in a `<div className="mx-auto max-w-6xl">`. Import `cn` from `@/lib/utils`.

**2. Create stub `src/components/layout/Header.tsx`** as a placeholder client component:

```tsx
"use client";
export function Header() {
  return (
    <header className="fixed top-0 z-50 w-full bg-navy-900 p-4">
      <div className="mx-auto flex max-w-6xl items-center justify-between">
        <span className="text-lg font-bold text-white">Prashanna Sangroula</span>
        <span className="text-sm text-gold-400">Header placeholder - replaced in Plan 02</span>
      </div>
    </header>
  );
}
```

This uses the navy/gold tokens to visually confirm they work. Plan 02 will completely replace this file.

**3. Create stub `src/components/layout/Footer.tsx`** as a placeholder server component:

```tsx
export function Footer() {
  return (
    <footer className="bg-navy-950 p-8 text-center text-sm text-navy-200">
      Footer placeholder - replaced in Plan 03
    </footer>
  );
}
```

**4. Update `src/app/layout.tsx`:**
- Import `Header` from `@/components/layout/Header` and `Footer` from `@/components/layout/Footer`
- Update metadata: `title: "Prashanna Sangroula | Mortgage Loan Officer"`, `description: "Licensed Mortgage Loan Officer NMLS #2528620 at Loan Factory, Inc. Specializing in Conventional, FHA, VA, USDA, Jumbo, and investment loans."`
- Add `data-scroll-behavior="smooth"` attribute to `<html>` tag
- Add `font-sans` to the body className (alongside existing font variables and antialiased)
- Render `<Header />` before `<main>{children}</main>` and `<Footer />` after it inside the body
- Keep existing Geist font setup exactly as-is

**5. Replace `src/app/page.tsx`** entirely. Import `SectionWrapper` from `@/components/layout/SectionWrapper`. Compose all 7 sections in order:

- `hero`: no `alternate`, add `className="pt-20"` to account for fixed header height, inner div with `min-h-[80vh] flex items-center justify-center`, placeholder text "Hero section - Phase 2" using `text-2xl font-bold text-muted-foreground`
- `services`: `alternate` prop, placeholder text "Services section - Phase 2"
- `about`: no `alternate`, placeholder text "About section - Phase 2"
- `testimonials`: `alternate` prop, placeholder text "Testimonials section - Phase 2"
- `calculator`: no `alternate`, placeholder text "Calculator section - Phase 3"
- `faq`: `alternate` prop, placeholder text "FAQ section - Phase 3"
- `contact`: no `alternate`, placeholder text "Contact section - Phase 3"

Each placeholder should have consistent styling: `<h2 className="text-2xl font-bold text-muted-foreground">{Section Name}</h2><p className="mt-2 text-muted-foreground">Coming in Phase {N}</p>`.

Use SECTION_IDS from `@/lib/constants` for the id values.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>SectionWrapper renders sections with alternating backgrounds. Layout.tsx imports and renders Header above main and Footer below. Page.tsx shows 7 named section placeholders. Build and lint pass. Navigating to /#services in browser would scroll to the services section (manual verification deferred to final checkpoint).</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes with zero errors
- `src/lib/constants.ts` exports CONTACT, COMPLIANCE, NAV_ITEMS, SOCIAL_LINKS, SECTION_IDS
- `src/app/globals.css` contains `--color-navy-` and `--color-gold-` custom properties
- `src/app/page.tsx` renders 7 sections with IDs: hero, services, about, testimonials, calculator, faq, contact
- `src/app/layout.tsx` has `data-scroll-behavior="smooth"` and renders Header + main + Footer
</verification>

<success_criteria>
The design token system generates working Tailwind classes (bg-navy-900, text-gold-500, etc.), all constants are centralized in one file, the page shows 7 scrollable section anchors with alternating backgrounds, and the root layout is wired with Header/Footer stubs. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-compliance/01-01-SUMMARY.md`
</output>
