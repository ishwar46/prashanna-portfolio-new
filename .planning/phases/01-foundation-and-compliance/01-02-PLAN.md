---
phase: 01-foundation-and-compliance
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/ui/sheet.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-03
  - NAV-04
  - COMP-01

must_haves:
  truths:
    - "Header is fixed at the top of the viewport and stays visible while scrolling"
    - "Header starts transparent over the hero and transitions to solid navy-900 with shadow after scrolling ~50px"
    - "Header displays the text wordmark 'Prashanna Sangroula' on the left"
    - "Header displays navigation links (Services, About, Testimonials, Calculator, FAQ, Contact) visible on desktop"
    - "Header displays phone number (571) 222-5555 as a tappable CTA button on the right"
    - "Phone number links to tel:+15712225555 (E.164 format)"
    - "NMLS #2528620 is displayed in the header as small text"
    - "On mobile screens (<768px), navigation links are replaced by a hamburger menu icon"
    - "Tapping the hamburger opens a Sheet slide-in panel with all navigation links"
    - "Phone number stays visible on mobile without opening the hamburger menu"
    - "Clicking a nav link in the mobile menu closes the Sheet and scrolls to the correct section"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky transparent-to-solid header with wordmark, nav links, phone CTA, NMLS display"
      min_lines: 40
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Sheet-based hamburger menu for mobile viewports"
      min_lines: 25
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component (Radix/Base UI Dialog primitive)"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/constants.ts"
      via: "imports NAV_ITEMS, CONTACT, COMPLIANCE"
      pattern: "import.*constants"
    - from: "src/components/layout/MobileNav.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "imports Sheet, SheetContent, SheetTrigger, SheetTitle"
      pattern: "import.*sheet"
    - from: "src/components/layout/MobileNav.tsx"
      to: "src/lib/constants.ts"
      via: "imports NAV_ITEMS for mobile menu links"
      pattern: "NAV_ITEMS"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/MobileNav.tsx"
      via: "renders MobileNav inside header for mobile breakpoint"
      pattern: "<MobileNav"

user_setup: []
---

<objective>
Build the sticky header with transparent-to-solid scroll behavior, desktop navigation links, phone CTA, NMLS display, and a Sheet-based mobile hamburger menu.

Purpose: The header is the primary navigation and trust element -- visitors see the brand name, NMLS credentials, and phone number immediately. The mobile hamburger menu ensures usability on all viewports.

Output: Fully functional Header.tsx and MobileNav.tsx that replace the Plan 01 stubs. The shadcn Sheet component is installed for the mobile menu.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-compliance/01-CONTEXT.md
@.planning/phases/01-foundation-and-compliance/01-RESEARCH.md
@.planning/phases/01-foundation-and-compliance/01-01-SUMMARY.md

@src/lib/constants.ts
@src/lib/utils.ts
@src/components/ui/button.tsx
@src/app/globals.css

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From src/lib/constants.ts:
```typescript
export const CONTACT = {
  phone: "(571) 222-5555",
  phoneHref: "tel:+15712225555",
  email: "prashanna@loanfactory.com",
  emailHref: "mailto:prashanna@loanfactory.com",
} as const;

export const COMPLIANCE = {
  personalNmls: "NMLS #2528620",
  companyNmls: "Loan Factory, Inc. NMLS #320841",
  equalHousing: "Equal Housing Lender",
  disclaimer: "Not a commitment to lend. Programs, rates, terms, and conditions are subject to change without notice. All loans subject to credit approval.",
} as const;

export const NAV_ITEMS = [
  { label: "Services", href: "#services" },
  { label: "About", href: "#about" },
  { label: "Testimonials", href: "#testimonials" },
  { label: "Calculator", href: "#calculator" },
  { label: "FAQ", href: "#faq" },
  { label: "Contact", href: "#contact" },
] as const;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
// Variants: default, outline, secondary, ghost, destructive, link
// Sizes: default, xs, sm, lg, icon, icon-xs, icon-sm, icon-lg
```

Design tokens available (from globals.css @theme):
- Navy: bg-navy-50 through bg-navy-950, text-navy-*, border-navy-*
- Gold: bg-gold-50 through bg-gold-900, text-gold-*, border-gold-*
- Semantic: bg-primary (navy-900), text-accent (gold-500), bg-secondary (light navy tint)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Sheet and create MobileNav component</name>
  <files>src/components/ui/sheet.tsx, src/components/layout/MobileNav.tsx</files>
  <action>
**1. Install the shadcn Sheet component:**
```bash
npx shadcn@latest add sheet
```
This generates `src/components/ui/sheet.tsx` using the `@base-ui/react` Dialog primitive (per the base-nova style in components.json).

**2. Create `src/components/layout/MobileNav.tsx`** as a client component (`"use client"`):

Import: `Sheet, SheetContent, SheetTrigger, SheetTitle` from `@/components/ui/sheet`, `Button` from `@/components/ui/button`, `Menu` icon from `lucide-react`, `NAV_ITEMS` from `@/lib/constants`, and `useState` from React.

The component:
- Renders a `<Sheet>` that is controlled via `open`/`onOpenChange` state (so we can close it programmatically when a nav link is clicked)
- `<SheetTrigger asChild>` wraps a `<Button variant="ghost" size="icon">` with className `"md:hidden text-white"` and `aria-label="Open navigation menu"`. Inside the Button, render `<Menu className="size-5" />`
- `<SheetContent side="right">` with className `"bg-navy-900 border-navy-800"`
- Inside SheetContent: `<SheetTitle className="sr-only">Navigation</SheetTitle>` for accessibility
- Then a `<nav className="flex flex-col gap-6 pt-8">` containing `NAV_ITEMS.map()` rendering `<a>` tags
- Each nav link: `href={item.href}`, `onClick={() => setOpen(false)}` to close the Sheet on click, className `"text-lg font-medium text-navy-100 transition-colors hover:text-gold-400"`
- Export the component as a named export: `export function MobileNav()`

The `onClick` handler calls `setOpen(false)` so the Sheet closes when a nav link is tapped, allowing smooth scroll to proceed.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && test -f src/components/ui/sheet.tsx && echo "Sheet exists" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>sheet.tsx exists in components/ui. MobileNav.tsx renders a hamburger trigger (hidden on md+) that opens a right-side Sheet with all 6 navigation links. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the full Header component with scroll behavior</name>
  <files>src/components/layout/Header.tsx</files>
  <action>
**Replace the stub `src/components/layout/Header.tsx`** with the full implementation as a client component (`"use client"`).

Imports: `useState`, `useEffect` from React. `cn` from `@/lib/utils`. `Button` from `@/components/ui/button`. `Phone` icon from `lucide-react`. `NAV_ITEMS`, `CONTACT`, `COMPLIANCE` from `@/lib/constants`. `MobileNav` from `@/components/layout/MobileNav`.

**Scroll detection:**
- `const [scrolled, setScrolled] = useState(false)`
- `useEffect` adds a passive scroll listener that sets `scrolled = window.scrollY > 50`
- Cleanup removes the listener

**Header element:**
- `<header>` with className using `cn()`:
  - Always: `"fixed top-0 z-50 w-full transition-all duration-300"`
  - When scrolled: `"bg-navy-900/95 shadow-lg backdrop-blur-sm"`
  - When NOT scrolled: `"bg-transparent"`

**Inner container:** `<div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">`

**Left side:** A flex container with:
- `<a href="#hero" className="flex flex-col">` containing:
  - `<span className="text-lg font-bold tracking-tight text-white">Prashanna Sangroula</span>` (the text wordmark per user decision)
  - `<span className="text-[10px] tracking-wide text-navy-300">{COMPLIANCE.personalNmls}</span>` (NMLS display, small text near wordmark per user decision)

**Center/Right navigation (desktop only):** `<nav className="hidden items-center gap-6 md:flex">`
- Map over `NAV_ITEMS` rendering `<a key={item.href} href={item.href} className="text-sm font-medium text-navy-100 transition-colors hover:text-gold-400">{item.label}</a>`

**Right side:** A flex container with `gap-3 items-center`:
- Phone CTA button: `<Button variant="outline" size="sm" asChild>` wrapping `<a href={CONTACT.phoneHref} className="hidden items-center gap-1.5 border-gold-500/50 text-gold-400 hover:bg-gold-500/10 hover:text-gold-300 sm:flex">` with `<Phone className="size-3.5" />` and `{CONTACT.phone}` text. The `hidden sm:flex` ensures the full phone button shows on sm+ screens.
- On very small screens (<sm), show a compact phone icon-only button: `<Button variant="ghost" size="icon-sm" asChild className="sm:hidden text-gold-400">` wrapping `<a href={CONTACT.phoneHref} aria-label="Call Prashanna"><Phone className="size-4" /></a>`
- `<MobileNav />` (renders the hamburger icon, hidden on md+)

Export as named export: `export function Header()`

**Key details per user decisions:**
- Wordmark is text, not a logo image
- Phone number stays visible on mobile (icon-only on <sm, full on sm+)
- Hamburger menu is rendered via MobileNav (hidden on md+)
- NMLS displayed as small text near the wordmark
- Transparent background on hero, solid with shadow on scroll
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>Header.tsx is a fully functional client component with: transparent-to-solid scroll transition, text wordmark with NMLS, desktop nav links, phone CTA button (responsive), and MobileNav hamburger. Build and lint pass.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npm run lint` passes with zero errors
- `src/components/ui/sheet.tsx` exists (shadcn Sheet installed)
- `src/components/layout/Header.tsx` imports from constants.ts (NAV_ITEMS, CONTACT, COMPLIANCE)
- `src/components/layout/MobileNav.tsx` uses Sheet component and maps NAV_ITEMS
- Header.tsx contains scroll detection logic (useState + useEffect + window.scrollY)
- Header.tsx uses `cn()` for conditional transparent/solid styling
- Phone number href is `tel:+15712225555` (E.164 format)
</verification>

<success_criteria>
The header is fixed at the top, starts transparent, transitions to solid navy with shadow on scroll. It shows "Prashanna Sangroula" wordmark with NMLS #2528620, desktop nav links, and a phone CTA button. On mobile, nav links collapse into a hamburger that opens a Sheet side panel. Phone number remains accessible on all screen sizes. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-compliance/01-02-SUMMARY.md`
</output>
