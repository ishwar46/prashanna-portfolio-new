---
phase: 05-polish-and-launch-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/AnimatedSection.tsx
  - src/components/Providers.tsx
  - src/app/layout.tsx
  - src/app/icon.tsx
  - src/app/apple-icon.tsx
  - src/app/favicon.ico
  - src/components/sections/ServicesSection.tsx
  - src/components/sections/CalculatorSection.tsx
  - src/components/sections/ContactSection.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/TestimonialsSection.tsx
  - src/components/sections/FAQSection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Scrolling down the page reveals each section with a subtle fade-in-up animation"
    - "Animations play only once per section and do not replay on scroll-back"
    - "With prefers-reduced-motion enabled in OS settings, no animations play"
    - "All images on the page use next/image (no raw img tags)"
    - "Hero image loads eagerly with priority; all other images load lazily"
    - "Browser tab shows a navy PS monogram favicon"
  artifacts:
    - path: "src/components/ui/AnimatedSection.tsx"
      provides: "Reusable client wrapper for scroll-triggered entrance animations"
      exports: ["AnimatedSection"]
    - path: "src/components/Providers.tsx"
      provides: "MotionConfig wrapper with reducedMotion=user"
      exports: ["Providers"]
    - path: "src/app/icon.tsx"
      provides: "PNG favicon generation via next/og ImageResponse"
    - path: "src/app/apple-icon.tsx"
      provides: "Apple touch icon generation via next/og ImageResponse"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Providers.tsx"
      via: "Providers wrapping body content"
      pattern: "<Providers>"
    - from: "src/components/sections/AboutSection.tsx"
      to: "src/components/ui/AnimatedSection.tsx"
      via: "AnimatedSection wrapping section content"
      pattern: "AnimatedSection"
    - from: "src/components/sections/ServicesSection.tsx"
      to: "motion/react"
      via: "motion.div whileInView on section content"
      pattern: "whileInView"
---

<objective>
Add scroll-triggered entrance animations to all below-fold sections, set up prefers-reduced-motion support, generate PS monogram favicon, and audit image optimization.

Purpose: Make the site feel polished and professional with subtle animations while ensuring accessibility and proper image optimization.
Output: AnimatedSection component, Providers wrapper, favicon files, animated sections.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-launch-validation/05-RESEARCH.md

@src/app/layout.tsx
@src/app/page.tsx
@src/components/sections/ServicesSection.tsx
@src/components/sections/AboutSection.tsx
@src/components/sections/TestimonialsSection.tsx
@src/components/sections/CalculatorSection.tsx
@src/components/sections/FAQSection.tsx
@src/components/sections/ContactSection.tsx

<interfaces>
<!-- Section rendering model (determines animation approach): -->
<!-- Client Components (already "use client"): ServicesSection, CalculatorSection, ContactSection -->
<!--   -> Add motion.div whileInView directly inside these components -->
<!-- Server Components (no "use client"): AboutSection, TestimonialsSection, FAQSection -->
<!--   -> Wrap content with AnimatedSection client component -->
<!-- HeroSection: Server Component, above-fold — NO animation (would hurt LCP) -->

<!-- Layout structure from layout.tsx: -->
<!-- <html> > <body> > <Header /> > <main>{children}</main> > <Footer /> > <Analytics /> > <SpeedInsights /> -->
<!-- Providers wrapper goes around body content (between body and Header) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedSection, Providers, and favicon</name>
  <files>src/components/ui/AnimatedSection.tsx, src/components/Providers.tsx, src/app/layout.tsx, src/app/icon.tsx, src/app/apple-icon.tsx, src/app/favicon.ico</files>
  <action>
1. Create `src/components/ui/AnimatedSection.tsx` — a "use client" component:
   - Import `motion` from `motion/react`
   - Props: `children: React.ReactNode`, `className?: string`, `delay?: number` (default 0)
   - Render a `motion.div` with:
     - `initial={{ opacity: 0, y: 20 }}`
     - `whileInView={{ opacity: 1, y: 0 }}`
     - `viewport={{ once: true, amount: 0.15 }}`
     - `transition={{ duration: 0.4, delay, ease: [0.25, 0.1, 0.25, 1] }}`
   - Pass through className

2. Create `src/components/Providers.tsx` — a "use client" component:
   - Import `MotionConfig` from `motion/react`
   - Render `<MotionConfig reducedMotion="user">{children}</MotionConfig>`
   - This globally respects the OS-level prefers-reduced-motion setting

3. Update `src/app/layout.tsx`:
   - Import `Providers` from `@/components/Providers`
   - Wrap the body content (Header, main, Footer) with `<Providers>` — place it inside `<body>` wrapping everything except Analytics and SpeedInsights
   - Keep Analytics and SpeedInsights outside Providers (they don't need motion config)

4. **Delete `src/app/favicon.ico`** — the existing file is the default Next.js favicon. Removing it ensures Next.js falls through to `icon.tsx` for the browser tab favicon. Run: `rm src/app/favicon.ico`

5. Create `src/app/icon.tsx` — generates a 32x32 PNG favicon:
   - Export `size = { width: 32, height: 32 }` and `contentType = "image/png"`
   - Use `ImageResponse` from `next/og` to render a navy rectangle (#0a1628) with gold text (#d4a843) reading "PS"
   - Font size ~18px, bold, centered, borderRadius 6

6. Create `src/app/apple-icon.tsx` — generates a 180x180 Apple touch icon:
   - Export `size = { width: 180, height: 180 }` and `contentType = "image/png"`
   - Same design as icon.tsx but scaled up: fontSize ~90px, borderRadius 32
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && test ! -f src/app/favicon.ico && echo "favicon.ico deleted — PASS" || echo "favicon.ico still exists — FAIL"</automated>
  </verify>
  <done>AnimatedSection component exists and exports correctly, Providers wraps layout body with MotionConfig reducedMotion="user", default favicon.ico is deleted, icon.tsx and apple-icon.tsx generate PS monogram favicons, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply scroll animations to all below-fold sections and audit images</name>
  <files>src/components/sections/ServicesSection.tsx, src/components/sections/CalculatorSection.tsx, src/components/sections/ContactSection.tsx, src/components/sections/AboutSection.tsx, src/components/sections/TestimonialsSection.tsx, src/components/sections/FAQSection.tsx, src/app/page.tsx</files>
  <action>
**For Client Components (ServicesSection, CalculatorSection, ContactSection):**
Add `motion.div` wrapper inside each component around the main content (inside the SectionWrapper):
- Import `motion` from `motion/react`
- Wrap the primary content block in `motion.div` with:
  - `initial={{ opacity: 0, y: 20 }}`
  - `whileInView={{ opacity: 1, y: 0 }}`
  - `viewport={{ once: true, amount: 0.1 }}`
  - `transition={{ duration: 0.35, ease: [0.25, 0.1, 0.25, 1] }}`
- For ServicesSection: optionally use staggered children on the card grid (containerVariants with staggerChildren: 0.08, itemVariants with opacity/y animation) for a cascading card reveal effect
- Do NOT add delay to the content — keep it snappy

**For Server Components (AboutSection, TestimonialsSection, FAQSection):**
Wrap the main content inside SectionWrapper with the `AnimatedSection` client component:
- Import `AnimatedSection` from `@/components/ui/AnimatedSection`
- Wrap the inner content (not the SectionWrapper itself, but its children) with `<AnimatedSection>`
- This preserves the Server Component rendering while adding client-side animation

**HeroSection: Do NOT touch.** It is above the fold and must render immediately for LCP.

**Image Audit:**
- Verify no raw `<img>` tags exist anywhere in src/ (all should use next/image `Image` component)
- Verify HeroSection's Image has `priority` prop
- Verify AboutSection's Image does NOT have `priority` (should lazy load by default)
- If any issues found, fix them

**Grep verification:** Run `grep -r "<img " src/` to confirm zero raw img tags.
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && grep -r "<img " src/ || echo "No raw img tags found — PASS"</automated>
  </verify>
  <done>All 6 below-fold sections have scroll-triggered entrance animations. Hero has no animation. No raw img tags exist. Build passes clean.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `grep -r "<img " src/` returns no results (all images use next/image)
3. `src/app/favicon.ico` does not exist (deleted, replaced by icon.tsx)
4. Visual: scrolling the page reveals sections with subtle fade-in-up animations
5. Visual: with Chrome DevTools > Rendering > prefers-reduced-motion: reduce, animations are suppressed
6. Browser tab shows PS monogram favicon
</verification>

<success_criteria>
- All below-fold sections animate on scroll entry (fade-in-up, once only)
- prefers-reduced-motion suppresses all animations via MotionConfig
- PS monogram favicon visible in browser tab (default favicon.ico removed)
- All images use next/image, hero has priority, others lazy
- Production build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-launch-validation/05-01-SUMMARY.md`
</output>
