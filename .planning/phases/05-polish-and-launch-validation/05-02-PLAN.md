---
phase: 05-polish-and-launch-validation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/globals.css
autonomous: false
requirements:
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "Every section is fully usable at 375px viewport width with no horizontal scrolling"
    - "All tap targets are at least 44px in size"
    - "LCP is below 2.5 seconds on simulated mobile Lighthouse audit"
    - "CLS is below 0.1 on simulated mobile Lighthouse audit"
    - "Production build completes with no errors or console warnings"
  artifacts:
    - path: "build output"
      provides: "Clean production build"
  key_links:
    - from: "npm run build"
      to: "all source files"
      via: "Next.js build pipeline"
      pattern: "Compiled successfully"
---

<objective>
Run mobile responsiveness audit at 375px viewport and Lighthouse performance audit to validate Core Web Vitals targets (LCP < 2.5s, CLS < 0.1). Fix any issues found.

Purpose: Ensure the site is production-ready with no mobile layout issues and meets performance targets before launch.
Output: Verified mobile quality and performance metrics, any CSS fixes applied.
</objective>

<execution_context>
@/Users/ishwarchaudhary/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishwarchaudhary/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-launch-validation/05-01-SUMMARY.md
@.planning/phases/05-polish-and-launch-validation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and automated quality checks</name>
  <files>src/app/globals.css</files>
  <action>
1. Run `npm run build` and verify it completes with zero errors and no critical warnings.

2. Run `npm run lint` and fix any linting issues.

3. Check for .env.example — verify it documents all required environment variables (NEXT_PUBLIC_SITE_URL, RESEND_API_KEY if contact form uses it). Create or update if missing.

4. Verify no `console.log` or `console.error` statements exist in production code:
   - `grep -r "console\." src/ --include="*.tsx" --include="*.ts"` — remove any debug logging

5. Check the production build output for any render-blocking resource warnings or size concerns.

6. If any mobile layout issues are identifiable from code inspection (e.g., fixed widths that could overflow on 375px, missing responsive classes), fix them in the relevant files. Common checks:
   - No fixed pixel widths larger than 375px on any content container
   - All text wraps properly (no nowrap that could cause overflow)
   - Calculator inputs and form fields are full-width on mobile
   - Service cards stack to single column on small screens

7. Add `overflow-x: hidden` to the html/body in globals.css as a safety net against any horizontal scroll (only if not already present).
  </action>
  <verify>
    <automated>cd /Users/ishwarchaudhary/projects/prashanna-portfolio && npm run build 2>&1 | tail -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Production build passes clean, no lint errors, no console.log in production code, .env.example is current, no obvious mobile overflow issues in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final launch validation checkpoint</name>
  <action>Human verifies the complete site: scroll animations, favicon, mobile responsiveness at 375px, and Core Web Vitals performance.</action>
  <verify>User confirms all checks pass by typing "approved".</verify>
  <done>User approves animations, favicon, mobile layout, and performance metrics.</done>
  <what-built>Complete site with scroll animations, favicon, image optimization, and mobile responsiveness across all 5 phases of development.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **Animations:** Scroll down the page slowly — each section should fade in subtly as it enters the viewport. Scroll back up — animations should NOT replay.
    3. **Reduced motion:** Open Chrome DevTools > Rendering panel > check "Emulate CSS media feature prefers-reduced-motion: reduce". Reload and scroll — no animations should play.
    4. **Favicon:** Check the browser tab — should show a navy square with gold "PS" text.
    5. **Mobile (375px):** Open DevTools > Toggle device toolbar > select iPhone SE (375px). Scroll through entire page:
       - No horizontal scrollbar at any point
       - All text readable, no overflow or clipping
       - All buttons/links tappable (visually at least 44px)
       - Calculator, FAQ accordion, and contact form all usable
    6. **Performance (optional but recommended):** Run `npm run build && npm start`, then open Chrome DevTools > Lighthouse tab > run Performance audit on mobile preset. Check LCP < 2.5s and CLS < 0.1.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npm run lint` passes with no errors
3. No `console.log` in production source files
4. Human verification of animations, favicon, mobile layout, and performance
</verification>

<success_criteria>
- Production build is clean (no errors, no critical warnings)
- Site is fully usable at 375px with no horizontal scroll
- All tap targets are adequately sized
- LCP < 2.5s and CLS < 0.1 (validated by Lighthouse or human spot-check)
- User approves final visual and functional quality
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-launch-validation/05-02-SUMMARY.md`
</output>
